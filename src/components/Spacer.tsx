import React from "react";

const Spacer = ({ children }: any) => {
  return (
    <div className="w-full h-full max-w-[1350px] mx-auto px-[5%]">
      {children}
    </div>
  );
};

export default Spacer;
